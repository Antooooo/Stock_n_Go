<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stock_n_go">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".scanner" />
        <activity android:name=".suppressionproduit" />
        <activity android:name=".descriptif_produit" />
        <activity android:name=".corpsproduit" />
        <activity android:name=".voirliste" />
        <activity android:name=".inscription" />
        <activity android:name=".connexion" />
        <activity
            android:name=".ui.login.pageLogin"
            android:label="@string/title_activity_page_login" />
        <activity android:name=".nouvelle_fiche" />
        <activity android:name=".mesfiches" />
        <activity
            android:name=".MainActivity"
            android:label=""
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>